{
  "name": "cog-monorepo",
  "description": "Cog monorepo — packages live under packages/*. The actual CLI is `cog`.",
  "version": "0.0.1",
  "private": true,
  "keywords": [],
  "author": "connorlittleton",
  "license": "MIT",
  "scripts": {
    "format": "biome format --write",
    "lint": "biome lint --write",
    "check": "biome check --write",
    "ci": "biome ci",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
